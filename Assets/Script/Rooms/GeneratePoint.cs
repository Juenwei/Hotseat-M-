using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GeneratePoint : MonoBehaviour
{
    //[SerializeField]private enum cubeFaceDirection { forward,backward,left,right}
    //[SerializeField]private cubeFaceDirection cubeFace;
    //[SerializeField] private GameObject CubeObject;

    //[SerializeField]private Transform cubeSize;
    //private SpawnRoom _spawnRoom;
    //[SerializeField]private GameObject startPointObject,endPointObject;

    //void Awake()
    //{
    //    //Currently this is prefabs , so either use find or use singetlon
    //    //_spawnRoom = FindObjectOfType<SpawnRoom>().GetComponent<SpawnRoom>();
    //    //float spawnPointRotation = _spawnRoom.GetRotation();
    //    //transform.localRotation = Quaternion.Euler(0, spawnPointRotation, 0);
    //    //_spawnRoom.AssignStartPoint(startPointObject);
    //    //_spawnRoom.AssignEndPoint(endPointObject);
    //}

    //public float GetCubeSize()
    //{
    //    return cubeSize.transform.localScale.z;
    //}

   
}
